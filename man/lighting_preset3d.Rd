% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lights.R
\name{lighting_preset3d}
\alias{lighting_preset3d}
\title{Add a named lighting preset}
\usage{
lighting_preset3d(
  scene = NULL,
  preset = c("three_point", "rembrandt", "butterfly", "split"),
  x = NULL,
  center = NULL,
  radius = NULL,
  add = TRUE,
  axes = TRUE,
  nticks = 5
)
}
\arguments{
\item{scene}{Optional Babylonian htmlwidget / `babylon_scene` when using
`lighting_preset3d()` in a pipe, or an unnamed alias for `preset` in
non-piped calls.}

\item{preset}{Lighting preset name. Supported values are `"three_point"`,
`"rembrandt"`, `"butterfly"`, and `"split"`.}

\item{x}{Optional `mesh3d` or `babylon_mesh` used to estimate scene center
and scale for the preset.}

\item{center}{Optional manual scene center. Overrides the center derived from
`x`.}

\item{radius}{Optional manual scene radius. Overrides the radius derived from
`x`.}

\item{add}{Whether to add the preset lights to the current scene. Use
`add = FALSE` to start a fresh scene with just the preset lights.}

\item{axes}{Whether to draw lightweight scene axes, ticks, labels, and a
bounding box.}

\item{nticks}{Approximate number of tick marks per axis when `axes = TRUE`.}
}
\description{
This creates a small rig of lights using a familiar studio-lighting layout.
Presets mirror the scene editor options and are positioned relative to a
target mesh when supplied.
}
